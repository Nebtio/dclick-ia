---
// Dclick IA – Landing Page restructurée (3 offres)
// Astro 4 + CSS custom
---

<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Consultant IA pour TPE & PME — Automatisation & IA | Dclick IA, Gien</title>
  <meta name="description" content="Automatisez vos devis, relances et saisies avec l'IA. Résultats en moins de 30 jours, sans formation lourde. Diagnostic gratuit gratuit (30 min). Consultant indépendant basé à Gien, Centre-Val de Loire." />
  <link rel="icon" type="image/png" href="/LogoDclickV2.png" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Sora:wght@300;400;500;600;700&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet" />

  <style>
    :root {
      --clr-bg: #0A0A0A;
      --clr-surface: #141414;
      --clr-surface-alt: #1a1e28;
      --clr-accent: #2E7BD4;
      --clr-accent-hover: #3B82F6;
      --clr-accent-glow: rgba(46, 123, 212, 0.25);
      --clr-text: #e8e6e3;
      --clr-text-muted: #A0AEC0;
      --clr-border: #222222;
      --clr-success: #34d399;
      --clr-info: #60a5fa;
      --clr-iot: #a78bfa;
      --font-heading: 'Sora', sans-serif;
      --font-mono: 'Space Mono', monospace;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body {
      font-family: var(--font-heading);
      background: var(--clr-bg);
      color: var(--clr-text);
      line-height: 1.6;
      overflow-x: hidden;
    }

    /* — Utility — */
    .container { max-width: 1140px; margin: 0 auto; padding: 0 1.5rem; }
    .text-accent { color: var(--clr-accent); }
    .text-muted { color: var(--clr-text-muted); }
    .font-mono { font-family: var(--font-mono); }

    /* — Animations — */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes glow {
      0%, 100% { box-shadow: 0 0 20px var(--clr-accent-glow); }
      50% { box-shadow: 0 0 40px var(--clr-accent-glow), 0 0 60px rgba(46,123,212,0.1); }
    }
    .animate-fade-up { opacity: 0; animation: fadeUp 0.7s ease-out forwards; }
    .delay-1 { animation-delay: 0.1s; }
    .delay-2 { animation-delay: 0.2s; }
    .delay-3 { animation-delay: 0.3s; }
    .delay-4 { animation-delay: 0.4s; }

    /* — Nav — */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 50;
      background: rgba(12, 14, 19, 0.85);
      backdrop-filter: blur(16px);
      border-bottom: 1px solid var(--clr-border);
      overflow: visible;
    }
    nav .nav-inner {
      display: flex; align-items: center; justify-content: space-between;
      max-width: 1140px; margin: 0 auto; padding: 0.75rem 1.5rem;
    }
    nav img { height: 40px; transform: scale(4); transform-origin: left center; margin-left: 90px; margin-right: 90px; }
    nav .nav-links { display: flex; gap: 2rem; align-items: center; }
    nav .nav-links a {
      color: var(--clr-text-muted); text-decoration: none;
      font-size: 0.9rem; font-weight: 500; transition: color 0.2s;
    }
    nav .nav-links a:hover { color: var(--clr-accent); }

    /* — Buttons — */
    .btn-primary {
      display: inline-flex; align-items: center; gap: 0.5rem;
      background: var(--clr-accent); color: var(--clr-bg);
      padding: 0.75rem 1.75rem; border-radius: 8px;
      font-weight: 600; font-size: 0.95rem;
      text-decoration: none; border: none; cursor: pointer;
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      background: var(--clr-accent-hover);
      transform: translateY(-2px);
      box-shadow: 0 8px 25px var(--clr-accent-glow);
    }
    .btn-secondary {
      display: inline-flex; align-items: center; gap: 0.5rem;
      background: transparent; color: var(--clr-accent);
      padding: 0.75rem 1.75rem; border-radius: 8px;
      font-weight: 600; font-size: 0.95rem;
      text-decoration: none; border: 1px solid var(--clr-accent);
      transition: all 0.3s ease;
    }
    .btn-secondary:hover { background: rgba(46,123,212,0.1); transform: translateY(-2px); }
    .btn-sm { padding: 0.5rem 1.25rem; font-size: 0.85rem; }

    /* — Hero — */
    .hero {
      min-height: 100vh; display: flex; align-items: center;
      padding: 8rem 0 4rem;
      background:
        radial-gradient(ellipse at 20% 50%, rgba(46,123,212,0.06) 0%, transparent 50%),
        radial-gradient(ellipse at 80% 20%, rgba(96,165,250,0.04) 0%, transparent 50%),
        var(--clr-bg);
    }
    .hero h1 {
      font-size: clamp(2.2rem, 5vw, 3.5rem);
      font-weight: 700; line-height: 1.15; margin-bottom: 1.25rem;
    }
    .hero .subtitle {
      font-size: 1.05rem; color: var(--clr-text-muted);
      margin-bottom: 0.75rem; letter-spacing: 0.5px;
    }
    .hero .tagline {
      font-size: 1.1rem; line-height: 1.7; max-width: 600px;
      margin-bottom: 1.5rem; border-left: 3px solid var(--clr-accent);
      padding-left: 1rem; color: var(--clr-text);
    }
    .hero .trust-signals {
      display: flex; gap: 1.5rem; flex-wrap: wrap;
      font-size: 0.85rem; color: var(--clr-text-muted); margin-bottom: 2rem;
    }
    .hero .trust-signals span::before { content: '◆ '; color: var(--clr-accent); }
    .hero .cta-group { display: flex; gap: 1rem; flex-wrap: wrap; }
    /* — Hero new elements — */
    .badge-location {
      display: inline-block; font-size: 0.8rem; font-weight: 600;
      color: var(--clr-accent); background: rgba(46,123,212,0.1);
      border: 1px solid rgba(46,123,212,0.3); border-radius: 100px;
      padding: 0.3rem 0.9rem; margin-bottom: 1.5rem;
      letter-spacing: 0.3px;
    }
    .hero-subtitle {
      font-size: 1.05rem; color: var(--clr-text-muted);
      line-height: 1.7; margin-bottom: 2rem; max-width: 580px;
    }
    .hero-cta { display: flex; gap: 1rem; flex-wrap: wrap; margin-bottom: 3rem; }
    .btn-outline {
      display: inline-flex; align-items: center; gap: 0.5rem;
      background: transparent; color: var(--clr-text);
      padding: 0.75rem 1.75rem; border-radius: 8px;
      font-weight: 600; font-size: 0.95rem;
      text-decoration: none; border: 1px solid var(--clr-border);
      transition: all 0.3s ease;
    }
    .btn-outline:hover { border-color: var(--clr-accent); color: var(--clr-accent); transform: translateY(-2px); }
    .hero-metrics {
      display: flex; gap: 2.5rem; flex-wrap: wrap;
      margin-bottom: 2rem; padding: 1.5rem 2rem;
      background: var(--clr-surface); border: 1px solid var(--clr-border);
      border-radius: 12px; width: fit-content;
    }
    .metric { display: flex; flex-direction: column; gap: 0.2rem; }
    .metric-value {
      font-size: 1.75rem; font-weight: 700; color: var(--clr-accent);
      font-family: var(--font-mono); line-height: 1;
    }
    .metric-label { font-size: 0.8rem; color: var(--clr-text-muted); }
    .hero-reassurance {
      font-size: 0.82rem; color: var(--clr-text-muted);
      border-left: 3px solid var(--clr-border); padding-left: 0.75rem;
    }

    /* — Section titles — */
    .section-title { font-size: clamp(1.6rem, 3.5vw, 2.2rem); font-weight: 700; margin-bottom: 0.75rem; }
    .section-subtitle { color: var(--clr-text-muted); font-size: 1.05rem; max-width: 600px; margin-bottom: 3rem; }
    section { padding: 5rem 0; }

    /* — Manifeste — */
    .manifeste {
      border-top: 1px solid var(--clr-border);
      border-bottom: 1px solid var(--clr-border);
      background: linear-gradient(135deg, rgba(46,123,212,0.04) 0%, transparent 60%);
    }
    .manifeste-inner {
      max-width: 760px; margin: 0 auto;
    }
    .manifeste-tag {
      font-family: var(--font-mono); font-size: 0.75rem; font-weight: 700;
      text-transform: uppercase; letter-spacing: 2px; color: var(--clr-accent);
      margin-bottom: 1.5rem; display: block;
    }
    .manifeste h2 {
      font-size: clamp(1.4rem, 3vw, 1.9rem); font-weight: 700;
      margin-bottom: 1.5rem; line-height: 1.3;
    }
    .manifeste .intro {
      font-size: 1.05rem; color: var(--clr-text-muted);
      line-height: 1.8; margin-bottom: 1.25rem;
    }
    .manifeste .role {
      font-size: 1rem; color: var(--clr-text-muted);
      line-height: 1.8; margin-bottom: 1.5rem;
    }
    .manifeste ul.manifeste-list {
      list-style: none; padding: 0; margin-bottom: 2rem;
    }
    .manifeste ul.manifeste-list li {
      font-size: 1rem; color: var(--clr-text);
      padding: 0.4rem 0 0.4rem 1.5rem; position: relative;
    }
    .manifeste ul.manifeste-list li::before {
      content: '→'; position: absolute; left: 0; color: var(--clr-accent);
    }
    .manifeste .signature {
      font-size: 0.95rem; color: var(--clr-text-muted);
      font-style: italic; border-left: 3px solid var(--clr-accent);
      padding-left: 1rem; line-height: 1.8;
    }
    .manifeste .signature strong { color: var(--clr-text); font-style: normal; }

    /* — Pain points — */
    .pain-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1.5rem; margin-top: 2.5rem; }
    .pain-card {
      background: var(--clr-surface); border: 1px solid var(--clr-border);
      border-radius: 12px; padding: 2rem; transition: all 0.3s ease;
    }
    .pain-card:hover { border-color: var(--clr-accent); transform: translateY(-4px); box-shadow: 0 12px 40px rgba(0,0,0,0.3); }
    .pain-card .pain-quote { font-style: italic; color: var(--clr-text-muted); margin-bottom: 1rem; font-size: 0.95rem; }
    .pain-card .pain-answer { color: var(--clr-accent); font-weight: 600; font-size: 0.95rem; }

    /* — Profils — */
    .profils-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1.5rem; }
    .profil-card {
      background: var(--clr-surface); border: 1px solid var(--clr-border);
      border-radius: 12px; padding: 2rem; position: relative; overflow: hidden; transition: all 0.3s ease;
    }
    .profil-card::before {
      content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px;
      background: var(--clr-accent); transform: scaleX(0); transform-origin: left; transition: transform 0.4s ease;
    }
    .profil-card:hover::before { transform: scaleX(1); }
    .profil-card:hover { border-color: var(--clr-accent); transform: translateY(-4px); }
    .profil-card h3 { font-size: 1.2rem; margin-bottom: 0.75rem; }
    .profil-card .profil-pain { font-style: italic; color: var(--clr-text-muted); font-size: 0.9rem; margin-bottom: 1rem; }
    .profil-card .profil-reco { font-family: var(--font-mono); font-size: 0.85rem; color: var(--clr-accent); line-height: 1.8; }

    /* — Parcours — */
    .parcours-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 2rem; }
    .parcours-card {
      background: var(--clr-surface); border: 1px solid var(--clr-border);
      border-radius: 16px; padding: 2.5rem 2rem; position: relative; overflow: hidden; transition: all 0.4s ease;
    }
    .parcours-card:hover { transform: translateY(-6px); box-shadow: 0 20px 50px rgba(0,0,0,0.3); }
    .parcours-card .step-number {
      font-family: var(--font-mono); font-size: 3.5rem; font-weight: 700;
      color: rgba(46,123,212,0.15); position: absolute; top: 1rem; right: 1.5rem; line-height: 1;
    }
    .parcours-card .step-badge {
      display: inline-block; font-family: var(--font-mono); font-size: 0.75rem; font-weight: 700;
      text-transform: uppercase; letter-spacing: 2px; padding: 0.3rem 0.75rem; border-radius: 4px; margin-bottom: 1rem;
    }
    .parcours-card.step-maitriser .step-badge { background: rgba(46,123,212,0.15); color: var(--clr-accent); }
    .parcours-card.step-maitriser:hover { border-color: var(--clr-accent); }
    .parcours-card.step-optimiser .step-badge { background: rgba(52,211,153,0.15); color: var(--clr-success); }
    .parcours-card.step-optimiser:hover { border-color: var(--clr-success); }
    .parcours-card.step-piloter .step-badge { background: rgba(167,139,250,0.15); color: var(--clr-iot); }
    .parcours-card.step-piloter:hover { border-color: var(--clr-iot); }
    .parcours-card h3 { font-size: 1.25rem; margin-bottom: 0.5rem; }
    .parcours-card .step-desc { color: var(--clr-text-muted); font-size: 0.95rem; margin-bottom: 1.25rem; }
    .parcours-card ul { list-style: none; padding: 0; }
    .parcours-card ul li { font-size: 0.9rem; color: var(--clr-text); padding: 0.35rem 0 0.35rem 1.5rem; position: relative; }
    .parcours-card ul li::before { content: '✓'; position: absolute; left: 0; color: var(--clr-accent); font-weight: 600; }
    .parcours-card.step-optimiser ul li::before { color: var(--clr-success); }
    .parcours-card.step-piloter ul li::before { color: var(--clr-iot); }
    .parcours-card .step-format { margin-top: 1.25rem; padding-top: 1rem; border-top: 1px solid var(--clr-border); font-size: 0.85rem; color: var(--clr-text-muted); }
    .parcours-card .step-result { margin-top: 0.75rem; font-family: var(--font-mono); font-size: 0.8rem; font-weight: 600; letter-spacing: 0.5px; }
    .parcours-card.step-maitriser .step-result { color: var(--clr-accent); }
    .parcours-card.step-optimiser .step-result { color: var(--clr-success); }
    .parcours-card.step-piloter .step-result { color: var(--clr-iot); }

    /* — IoT sub-grid — */
    .iot-sub-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 0.75rem; margin-top: 1rem; }
    .iot-sub-card { background: var(--clr-surface-alt); border-radius: 8px; padding: 0.75rem 0.5rem; text-align: center; min-width: 0; overflow: hidden; }
    .iot-sub-card h4 { font-size: 0.8rem; color: var(--clr-iot); margin-bottom: 0.4rem; font-weight: 700; white-space: nowrap; }
    .iot-sub-card p { font-size: 0.75rem; color: var(--clr-text-muted); line-height: 1.5; word-break: break-word; }

    /* — Cas d'usage — */
    .cas-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1.5rem; }
    .cas-card { background: var(--clr-surface); border: 1px solid var(--clr-border); border-radius: 12px; padding: 2rem; transition: all 0.3s ease; }
    .cas-card:hover { border-color: var(--clr-accent); transform: translateY(-3px); }
    .cas-card h4 { color: var(--clr-accent); font-size: 0.85rem; font-family: var(--font-mono); text-transform: uppercase; letter-spacing: 1px; margin-bottom: 0.75rem; }
    .cas-card p { font-size: 0.95rem; color: var(--clr-text-muted); font-style: italic; line-height: 1.6; }

    /* — CTA final — */
    .cta-section {
      text-align: center;
      background: radial-gradient(ellipse at center, rgba(46,123,212,0.08) 0%, transparent 60%), var(--clr-bg);
    }
    .cta-section .cta-box {
      background: var(--clr-surface); border: 1px solid var(--clr-border);
      border-radius: 16px; padding: 3rem 2rem; max-width: 700px; margin: 0 auto;
      animation: glow 4s ease-in-out infinite;
    }
    .cta-section h2 { margin-bottom: 1rem; }
    .cta-section p { color: var(--clr-text-muted); margin-bottom: 2rem; font-size: 1.05rem; }
    .cta-meta { display: flex; justify-content: center; gap: 2rem; flex-wrap: wrap; margin-top: 1.5rem; font-size: 0.85rem; color: var(--clr-text-muted); }

    /* — Footer — */
    footer { border-top: 1px solid var(--clr-border); padding: 2.5rem 0; }
    .footer-inner { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem; }
    .footer-brand { display: flex; align-items: center; gap: 0.75rem; }
    .footer-brand img { height: 32px; }
    .footer-brand span { font-size: 0.85rem; color: var(--clr-text-muted); }
    .footer-links { display: flex; gap: 1.5rem; }
    .footer-links a { color: var(--clr-text-muted); text-decoration: none; font-size: 0.8rem; transition: color 0.2s; }
    .footer-links a:hover { color: var(--clr-accent); }
    .footer-copy { width: 100%; text-align: center; font-size: 0.8rem; color: var(--clr-text-muted); margin-top: 1.5rem; padding-top: 1.5rem; border-top: 1px solid var(--clr-border); }

    /* — Mobile nav — */
    .nav-toggle { display: none; background: none; border: none; color: var(--clr-text); font-size: 1.5rem; cursor: pointer; }
    @media (max-width: 768px) {
      .nav-toggle { display: block; }
      nav .nav-links {
        display: none; position: absolute; top: 100%; left: 0; right: 0;
        background: var(--clr-surface); flex-direction: column; padding: 1.5rem;
        border-bottom: 1px solid var(--clr-border);
      }
      nav .nav-links.open { display: flex; }
      .iot-sub-grid { grid-template-columns: 1fr; }
    }

    /* — Fondateur — */
    .fondateur { border-top: 1px solid var(--clr-border); border-bottom: 1px solid var(--clr-border); }
    .fondateur-inner {
      display: grid; grid-template-columns: auto 1fr;
      gap: 3rem; align-items: center; max-width: 900px; margin: 0 auto;
    }
    .fondateur-photo {
      width: 180px; height: 180px; border-radius: 50%; object-fit: cover;
      border: 3px solid var(--clr-accent); box-shadow: 0 0 30px var(--clr-accent-glow);
    }
    @media (max-width: 768px) {
      .fondateur-inner { grid-template-columns: 1fr; text-align: center; }
      .fondateur-photo { margin: 0 auto; }
    }

    /* — RGPD badge — */
    .rgpd-badge {
      display: inline-flex; align-items: center; gap: 0.4rem;
      font-size: 0.75rem; color: var(--clr-success);
      background: rgba(52,211,153,0.08); border: 1px solid rgba(52,211,153,0.2);
      border-radius: 100px; padding: 0.25rem 0.7rem; margin-top: 1rem;
    }

    /* — Step price — */
    .step-price {
      font-family: var(--font-mono); font-size: 1.1rem; font-weight: 700;
      margin-top: 0.5rem; margin-bottom: 0.25rem;
    }
    .step-free {
      font-size: 0.85rem; color: var(--clr-success); font-weight: 600;
    }
    .parcours-card.step-maitriser .step-price { color: var(--clr-accent); }
    .parcours-card.step-optimiser .step-price { color: var(--clr-success); }
    .parcours-card.step-piloter .step-price { color: var(--clr-iot); }

    /* — FAQ — */
    .faq-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(420px, 1fr)); gap: 1.5rem; }
    .faq-item {
      background: var(--clr-surface); border: 1px solid var(--clr-border);
      border-radius: 12px; padding: 2rem; transition: all 0.3s ease;
    }
    .faq-item:hover { border-color: var(--clr-accent); }
    .faq-item h3 { font-size: 1rem; color: var(--clr-text); margin-bottom: 0.75rem; font-weight: 600; }
    .faq-item p { font-size: 0.95rem; color: var(--clr-text-muted); line-height: 1.7; }
    @media (max-width: 768px) { .faq-grid { grid-template-columns: 1fr; } }

    /* — Cas d'usage PAS — */
    .pas-label { font-family: var(--font-mono); font-size: 0.7rem; text-transform: uppercase; letter-spacing: 1.5px; color: var(--clr-accent); margin-bottom: 0.2rem; display: block; }
    .pas-block { margin-bottom: 0.9rem; }
    .pas-block p { font-size: 0.9rem; color: var(--clr-text-muted); margin: 0; line-height: 1.5; }
    .pas-block.action p, .pas-block.solution p { color: var(--clr-text); }
    .pas-block.solution .pas-label { color: var(--clr-success); }

    /* — Hero grid fusionné — */
    .hero-grid {
      display: grid; grid-template-columns: repeat(3, 1fr);
      gap: 1rem; margin-top: 2rem; width: 100%;
    }
    .hero-grid-card {
      background: var(--clr-surface); border: 1px solid var(--clr-border);
      border-radius: 12px; padding: 1.25rem; display: flex; flex-direction: column; gap: 0.25rem;
    }
    .hero-grid-label {
      font-size: 0.7rem; text-transform: uppercase; letter-spacing: 1.5px;
      color: var(--clr-text-muted); font-weight: 600;
    }
    .hero-grid-text {
      font-size: 0.95rem; color: var(--clr-text); font-weight: 500;
    }
    @media (max-width: 768px) {
      .hero-grid { grid-template-columns: repeat(2, 1fr); }
    }

    /* — Sticky CTA mobile — */
    .sticky-mobile-cta {
      display: none; position: fixed; bottom: 1rem; left: 50%;
      transform: translateX(-50%); z-index: 50;
    }
    .sticky-mobile-cta a {
      background: var(--clr-accent); color: #fff;
      padding: 0.75rem 1.5rem; border-radius: 100px;
      font-weight: 600; font-size: 0.9rem; text-decoration: none;
      white-space: nowrap; box-shadow: 0 8px 25px rgba(0,0,0,0.4);
    }
    @media (max-width: 768px) {
      .sticky-mobile-cta { display: block; }
    }
  </style>
</head>

<body>

  <!-- ——— NAV ——— -->
  <nav>
    <div class="nav-inner">
      <a href="/" style="color: var(--clr-text); text-decoration: none; font-size: 1.1rem; font-weight: 700;">Dclick <span style="color: var(--clr-accent);">IA</span></a>
      <button class="nav-toggle" aria-label="Menu" onclick="document.querySelector('.nav-links').classList.toggle('open')">☰</button>
      <div class="nav-links">
        <a href="#offres">Offres</a>
        <a href="#cas-usage">Cas d'usage</a>
        <a href="#contact">Contact</a>
        <a href="#diagnostic" class="btn-primary btn-sm">Demander mon diagnostic gratuit →</a>
      </div>
    </div>
  </nav>

  <!-- ——— HERO ——— -->
  <section class="hero">
    <div class="container">
      <span class="badge-location animate-fade-up">● Giennois &amp; Centre-Val de Loire</span>

      <h1 class="animate-fade-up delay-1">Libérez vos journées des tâches que personne ne veut faire.</h1>

      <p class="hero-subtitle animate-fade-up delay-2">
        Dclick IA automatise vos devis, relances et saisies administratives.<br />
        Sans projet complexe, sans formation lourde. Résultats en moins de 30 jours.
      </p>

      <div class="hero-cta animate-fade-up delay-3">
        <a href="#diagnostic" class="btn-primary">Demander mon diagnostic gratuit →</a>
      </div>

      <!-- BLOC FUSIONNÉ — Stats + Contexte -->
      <div class="hero-grid animate-fade-up delay-4">
        <div class="hero-grid-card">
          <span class="metric-value">10h+</span>
          <span class="metric-label">gagnées/mois sur l'administratif</span>
        </div>
        <div class="hero-grid-card">
          <span class="metric-value">&lt; 30j</span>
          <span class="metric-label">pour les premiers résultats</span>
        </div>
        <div class="hero-grid-card">
          <span class="metric-value">0</span>
          <span class="metric-label">formation lourde nécessaire</span>
        </div>
        <div class="hero-grid-card">
          <span class="hero-grid-label">Pour qui</span>
          <span class="hero-grid-text">TPE & PME · Artisans · Indépendants</span>
        </div>
        <div class="hero-grid-card">
          <span class="hero-grid-label">Secteurs</span>
          <span class="hero-grid-text">BTP · Commerce de proximité · Immobilier · Services aux entreprises</span>
        </div>
        <div class="hero-grid-card">
          <span class="hero-grid-label">Prochain pas</span>
          <a href="#diagnostic" class="hero-grid-text" style="color: var(--clr-accent); text-decoration: none;">Diagnostic gratuit 30 min →</a>
        </div>
      </div>

      <p class="hero-reassurance animate-fade-up delay-4">
        Hébergement français disponible · Conforme RGPD · Interlocuteur unique, basé à Gien
      </p>
    </div>
  </section>

  <!-- ——— MANIFESTE ——— -->
  <section class="manifeste">
    <div class="container">
      <div class="manifeste-inner">
        <span class="manifeste-tag">Une approche simple et concrète de l'IA</span>
        <h2>Aujourd'hui, l'intelligence artificielle devient accessible aux entreprises de toutes tailles.</h2>
        <p class="intro">
          Pourtant, beaucoup de dirigeants, d'artisans et d'équipes entendent parler d'IA… sans vraiment savoir par où commencer ni ce qui pourrait être utile dans leur quotidien.
        </p>
        <p class="role">C'est là que j'interviens.</p>
        <p class="intro">
          Mon rôle n'est pas de vous vendre une technologie compliquée, mais de vous aider à y voir plus clair : comprendre ce que l'IA peut réellement apporter à votre activité et identifier des usages simples, adaptés à votre métier.
        </p>
        <p class="intro">
          Ensuite, nous avançons pas à pas pour mettre en place des solutions qui s'intègrent naturellement dans votre façon de travailler.
        </p>
        <p class="signature">
          <strong>Sans jargon.<br />Sans usine à gaz.</strong><br />
          Juste des outils utiles et des exemples qui parlent à votre métier.
        </p>
      </div>
    </div>
  </section>

  <!-- ——— FONDATEUR ——— -->
  <section class="fondateur">
    <div class="container">
      <div class="fondateur-inner">
        <img src="/ben-portrait.jpg" alt="Ben — Fondateur Dclick IA" class="fondateur-photo" />
        <div class="fondateur-bio">
          <span class="manifeste-tag">Le fondateur</span>
          <h2 class="section-title">Bonjour, je suis <span class="text-accent">Benoit</span></h2>
          <p class="intro">Consultant en IA & automatisation — Giennois.</p>
          <p class="intro">Ancien électricien industriel en automatisme, aujourd'hui en production d'énergie, j'aide les TPE/PME à gagner du temps et en efficacité grâce à l'automatisation et à l'IA.</p>
          <p class="signature">Mon engagement : <strong>un premier résultat concret en moins de 30 jours.</strong></p>
        </div>
      </div>
    </div>
  </section>

  <!-- ——— PAIN POINTS ——— -->
  <section>
    <div class="container">
      <h2 class="section-title">Vous avez des questions sur l'IA. <span class="text-accent">Nous apportons des réponses.</span></h2>
      <div class="pain-grid">
        <div class="pain-card">
          <p class="pain-quote">"Je ne sais pas par où commencer"</p>
          <p class="pain-answer">→ On identifie ensemble les premières actions utiles</p>
        </div>
        <div class="pain-card">
          <p class="pain-quote">"Mon équipe résiste à l'IA"</p>
          <p class="pain-answer">→ On met en place des usages simples, acceptés par tous</p>
        </div>
        <div class="pain-card">
          <p class="pain-quote">"Je ne vois pas ce que ça m'apporte"</p>
          <p class="pain-answer">→ Vous repartez avec un cas concret déjà opérationnel</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ——— 3 PROFILS ——— -->
  <section>
    <div class="container">
      <h2 class="section-title">3 profils, <span class="text-accent">1 seul déclic</span></h2>
      <p class="section-subtitle">Trouvez l'accompagnement adapté à votre structure.</p>
      <div class="profils-grid">
        <div class="profil-card">
          <h3>Petite structure</h3>
          <p class="profil-pain">"Je n'ai pas le temps, je ne sais pas par où commencer"</p>
          <p class="profil-reco">
            → Ateliers pratiques&nbsp;(400€&nbsp;HT)<br />
            → Diagnostic gratuit&nbsp;(30&nbsp;min)
          </p>
        </div>
        <div class="profil-card">
          <h3>PME &amp; Services</h3>
          <p class="profil-pain">"Mon équipe résiste, je ne sais pas quoi automatiser"</p>
          <p class="profil-reco">
            → Diagnostic de rentabilité&nbsp;(1&nbsp;000€&nbsp;HT)<br />
            → puis Ateliers pratiques
          </p>
        </div>
        <div class="profil-card">
          <h3>Industrie &amp; Production</h3>
          <p class="profil-pain">"Mes données terrain ne servent à rien"</p>
          <p class="profil-reco">
            → Diagnostic de rentabilité<br />
            → IoT &amp; données terrain
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- ——— PARCOURS 3 ÉTAPES ——— -->
  <section id="offres">
    <div class="container">
      <h2 class="section-title">Le parcours en <span class="text-accent">3 étapes</span></h2>
      <p class="section-subtitle">De la prise en main à l'exploitation de vos données.</p>

      <div class="parcours-grid">

        <!-- ÉTAPE 01 — MAÎTRISER -->
        <div class="parcours-card step-maitriser">
          <span class="step-number">01</span>
          <span class="step-badge">Maîtriser</span>
          <h3>Ateliers pratiques</h3>
          <p class="step-desc">Rendre vos équipes autonomes sur les outils IA du quotidien et de création de contenu.</p>
          <ul>
            <li>Prise en main des assistants IA du quotidien</li>
            <li>Création de contenu assistée : texte, image, vidéo</li>
            <li>Bonnes pratiques et méthode de prompting</li>
            <li>Mini-charte IA interne pour encadrer les usages</li>
          </ul>
          <p class="step-price">400€ HT</p>
          <p class="step-format">½ journée ou 1 journée | Tous collaborateurs | Sur site ou à distance</p>
          <p class="step-result">Adoption sereine · Usage cadré · Autonomie rapide</p>
          <span class="rgpd-badge">✓ Conforme RGPD</span>
        </div>

        <!-- ÉTAPE 02 — OPTIMISER -->
        <div class="parcours-card step-optimiser">
          <span class="step-number">02</span>
          <span class="step-badge">Optimiser</span>
          <h3>Diagnostic de rentabilité</h3>
          <p class="step-desc">Identifier vos gains rapides et construire votre feuille de route IA.</p>
          <ul>
            <li>Diagnostic gratuit 30 min, sans engagement</li>
            <li>Audit complet des processus métier</li>
            <li>Cartographie des tâches répétitives et automatisables</li>
            <li>Recommandations priorisées + estimation ROI</li>
          </ul>
          <p class="step-free">Flash : gratuit</p>
          <p class="step-price">Audit complet : 1 000€ HT</p>
          <p class="step-result">Actions prioritaires · Gains estimés · Démarrage rapide</p>
          <span class="rgpd-badge">✓ Conforme RGPD</span>
        </div>

        <!-- ÉTAPE 03 — PILOTER (IoT) -->
        <div class="parcours-card step-piloter">
          <span class="step-number">03</span>
          <span class="step-badge">Pour aller plus loin</span>
          <h3>IoT &amp; données terrain</h3>
          <p class="step-desc">Captez, comprenez et anticipez grâce à vos données terrain.</p>
          <ul>
            <li>Diagnostic data et étude de faisabilité</li>
            <li>Capteurs connectés sur site</li>
            <li>Plan d'exploitation et de visualisation des données</li>
          </ul>
          <p class="step-result">Décisions pilotées par la donnée terrain</p>
          <span class="rgpd-badge">✓ Conforme RGPD</span>
        </div>

      </div>
    </div>
  </section>

  <!-- ——— CAS D'USAGE ——— -->
  <section id="cas-usage">
    <div class="container">
      <h2 class="section-title">3 cas d'usage concrets — <span class="text-accent">artisans &amp; services</span></h2>
      <p class="section-subtitle">Découvrez comment l'IA s'applique à votre quotidien.</p>

      <div class="cas-grid">
        <div class="cas-card">
          <h4>Artisan / TPE — Devis à la voix</h4>
          <div class="pas-block">
            <span class="pas-label">Problème</span>
            <p>45 minutes par devis, chaque jour. Du temps perdu sur de l'administratif.</p>
          </div>
          <div class="pas-block action">
            <span class="pas-label">Action</span>
            <p>L'artisan dicte ses notes au téléphone, en rentrant du chantier.</p>
          </div>
          <div class="pas-block solution">
            <span class="pas-label">Résultat</span>
            <p>Le devis est rédigé, mis en forme et prêt à envoyer en 3 minutes.</p>
          </div>
        </div>
        <div class="cas-card">
          <h4>Agence d'intérim — Tri de candidatures</h4>
          <div class="pas-block">
            <span class="pas-label">Problème</span>
            <p>2 heures pour trier 50 CV à chaque ouverture de poste.</p>
          </div>
          <div class="pas-block action">
            <span class="pas-label">Action</span>
            <p>Les CV sont analysés automatiquement selon les critères du poste.</p>
          </div>
          <div class="pas-block solution">
            <span class="pas-label">Résultat</span>
            <p>Les profils pertinents remontent en tête, classés et commentés, en quelques secondes.</p>
          </div>
        </div>
        <div class="cas-card">
          <h4>Direction PME — Réunion préparée</h4>
          <div class="pas-block">
            <span class="pas-label">Problème</span>
            <p>Réunion de direction sans chiffres à jour — décisions prises à l'aveugle.</p>
          </div>
          <div class="pas-block action">
            <span class="pas-label">Action</span>
            <p>Un rapport est généré automatiquement chaque lundi matin.</p>
          </div>
          <div class="pas-block solution">
            <span class="pas-label">Résultat</span>
            <p>Chiffres clés, tendances et alertes — prêts avant la réunion, sans saisie manuelle.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ——— FAQ ——— -->
  <section>
    <div class="container">
      <h2 class="section-title">Questions <span class="text-accent">fréquentes</span></h2>
      <p class="section-subtitle">Les vrais doutes, les vraies réponses.</p>
      <div class="faq-grid">
        <div class="faq-item">
          <h3>"Est-ce que c'est vraiment accessible pour mon activité&nbsp;?"</h3>
          <p>Oui. J'adapte chaque intervention à votre métier et votre niveau. Pas besoin d'être expert en informatique — si vous utilisez un smartphone, vous pouvez utiliser nos outils.</p>
        </div>
        <div class="faq-item">
          <h3>"Combien de temps avant de voir des résultats ?"</h3>
          <p>En général, moins de 30 jours pour les premiers gains. L'atelier pratique se fait en demi-journée. Le diagnostic gratuit, en 30 minutes chrono.</p>
        </div>
        <div class="faq-item">
          <h3>"Est-ce que mes données sont en sécurité ?"</h3>
          <p>Oui. Toutes nos solutions sont conformes RGPD. Hébergement français disponible. Je ne vends aucune donnée et travaille avec des outils européens quand c'est possible.</p>
        </div>
        <div class="faq-item">
          <h3>"Et si ça ne convient pas à mon équipe ?"</h3>
          <p>Le diagnostic gratuit est gratuit et sans engagement. Si l'IA ne vous apporte pas de valeur concrète, je ne vous propose rien. Simple.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ——— CTA FINAL ——— -->
  <section class="cta-section" id="contact">
    <div class="container">
      <div class="cta-box">
        <h2 class="section-title">Prêt à faire le <span class="text-accent">premier pas</span> ?</h2>
        <p>Commençons par un diagnostic gratuit de 30 min pour identifier vos 3 premiers gains IA.</p>
        <a href="#diagnostic" class="btn-primary">Demander mon diagnostic gratuit →</a>
        <div class="cta-meta">
          <span>Sans engagement</span>
        </div>
        <p style="margin-top: 1rem; font-size: 0.9rem; color: var(--clr-text-muted);">
          Giennois — Centre-Val de Loire
        </p>
      </div>
    </div>
  </section>

  <!-- ——— TALLY : DIAGNOSTIC GRATUIT ——— -->
  <section id="diagnostic" style="border-top: 1px solid var(--clr-border);">
    <div class="container" style="max-width: 760px;">
      <h2 class="section-title">Diagnostic gratuit <span class="text-accent">30 min</span></h2>
      <iframe
        data-tally-src="https://tally.so/embed/pbVa78?alignLeft=1&hideTitle=1&transparentBackground=1"
        loading="lazy"
        width="100%"
        height="500"
        frameborder="0"
        title="Diagnostic gratuit 30 min"
      ></iframe>
    </div>
  </section>

  <!-- ——— FOOTER ——— -->
  <footer>
    <div class="container">
      <div class="footer-inner">
        <div class="footer-brand">
          <span style="font-weight: 700; color: var(--clr-text); font-size: 1rem;">Dclick <span style="color: var(--clr-accent);">IA</span></span>
          <span>Automatisation simple. Résultats concrets.</span>
        </div>
        <div class="footer-links">
          <a href="/mentions-legales">Mentions légales</a>
          <a href="/politique-de-confidentialite">Politique de confidentialité</a>
        </div>
      </div>
      <p class="footer-copy">
        Données hébergées en France 🇫🇷 &nbsp;·&nbsp; © 2026 Dclick IA — Giennois, Centre-Val de Loire
      </p>
    </div>
  </footer>


  <!-- CTA STICKY MOBILE -->
  <div class="sticky-mobile-cta">
    <a href="#diagnostic">Diagnostic gratuit 30 min →</a>
  </div>

  <script src="https://tally.so/widgets/embed.js" is:inline></script>
</body>
</html>
